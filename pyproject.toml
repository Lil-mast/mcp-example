[project]
name = "mcp-example"
version = "1.0.0"
description = "A simple MCP server built with FastMCP"
requires-python = ">=3.10"
dependencies = [
    "fastmcp>=3.2.0",
]

[project.scripts]
server = "server:main"

[tool.uv]
dev-dependencies = [
    "anthropic>=0.96.0",
]
